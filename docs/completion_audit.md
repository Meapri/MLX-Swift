# Swift mlx-vlm Replacement Completion Audit

Objective: replace the Python `mlx-vlm` runtime with a 100% Swift compatibility/runtime layer while preserving existing MLX model directory compatibility and using official `mlx-swift` / `mlx-swift-lm` as the primary engine.

Status: not complete. The Swift server is usable for the main Gemma4/OpenAI/Ollama path and has now passed real Qwen2.5-VL, Qwen3-VL, SmolVLM2, FastVLM, and Gemma4 OCR smokes, but full Python `mlx-vlm` parity still has uncovered structured-output decoding gaps.

## Success Criteria

| Requirement | Current evidence | Status |
| --- | --- | --- |
| Python runtime removed from the working tree | `aeff99d Remove legacy Python mlx-vlm runtime`; no Python server files are part of the Swift runtime path | Done |
| Swift package builds without Python runtime | `swift build --disable-sandbox --jobs 18` passes | Done |
| Official `mlx-swift-lm` is the primary real inference engine | `MLXVLMUpstreamBackend` loads `VLMModelFactory.shared.loadContainer`, maps requests into `MLXLMCommon.UserInput`, and streams upstream `Generation` events | Done |
| Local MLX model directories remain inspectable | `ModelStore`, descriptor/config/tokenizer/processor/weight catalog commands are covered by `scripts/verify_swift_port.sh` | Done |
| OpenAI-compatible chat/completions/responses routes work | `scripts/verify_swift_port.sh` and `scripts/verify_real_gemma4_smoke.sh` cover chat, completions compatibility, responses, streaming, usage, and model endpoints | Mostly done |
| Ollama-compatible generate/chat/show/tags/ps/model-management/blob routes work | `2d5eaaf` implements model/blob management; `ce6e02e` adds real Gemma4 smoke coverage for these endpoints | Done for current server-local semantics |
| Tokenizer compatibility does not require Python for common sidecars | `d8a75db` adds WordPiece, `vocab.txt`, `tokenizer.tiktoken`, ByteLevel BPE, sidecar BPE, and WordLevel fallback coverage; the current tree adds Unigram `tokenizer.json` plus binary SentencePiece `tokenizer.model` greedy fallback coverage and parses binary SentencePiece `trainer_spec.model_type` so non-Unigram BPE/Word/Char variants are explicitly delegated to the real upstream Swift tokenizer integration | Done for dependency-free supported modes plus upstream-delegated non-Unigram SentencePiece |
| Gemma4 local MLX real backend works end-to-end | `scripts/verify_real_gemma4_smoke.sh` passes with the local Gemma4 E4B model, including chat, tools, image input, streaming, embeddings diagnostic, tokenize, and model/blob endpoints | Done |
| Audio/video/image metadata survives API normalization and upstream handoff | Self-test and `scripts/verify_swift_port.sh` cover metadata parsing/planning; mock upstream contracts include `UserInput.Video` | Partially verified |
| Video-capable model generation works end-to-end | `MLXVLM_REAL_QWEN25VL_MODEL=/Users/naen/Models/mlx-community-Qwen2.5-VL-3B-Instruct-3bit SWIFT_BUILD_JOBS=18 scripts/verify_real_qwen25vl_video_smoke.sh` passed after downloading `mlx-community/Qwen2.5-VL-3B-Instruct-3bit` locally; `MLXVLM_REAL_QWEN25VL_ALLOW_REMOTE=1 MLXVLM_REAL_QWEN25VL_DOWNLOAD_DIR=/Users/naen/Models/mlx-community-Qwen2.5-VL-3B-Instruct-3bit SWIFT_BUILD_JOBS=18 scripts/verify_real_qwen25vl_video_smoke.sh` also passed through the remote-download path; `MLXVLM_REAL_QWEN_VL_MODEL=mlx-community/Qwen3-VL-4B-Instruct-3bit MLXVLM_REAL_QWEN_VL_ALLOW_REMOTE=1 MLXVLM_REAL_QWEN_VL_DOWNLOAD_DIR=/Users/naen/Models/mlx-community-Qwen3-VL-4B-Instruct-3bit MLXVLM_REAL_QWEN_VL_LABEL=Qwen3-VL SWIFT_BUILD_JOBS=18 scripts/verify_real_qwen25vl_video_smoke.sh` downloaded `mlx-community/Qwen3-VL-4B-Instruct-3bit` and passed; `MLXVLM_REAL_QWEN_VL_MODEL=mlx-community/SmolVLM2-256M-Video-Instruct-mlx MLXVLM_REAL_QWEN_VL_ALLOW_REMOTE=1 MLXVLM_REAL_QWEN_VL_DOWNLOAD_DIR=/Users/naen/Models/mlx-community-SmolVLM2-256M-Video-Instruct-mlx MLXVLM_REAL_QWEN_VL_LABEL=SmolVLM2 SWIFT_BUILD_JOBS=18 scripts/verify_real_qwen25vl_video_smoke.sh` downloaded `mlx-community/SmolVLM2-256M-Video-Instruct-mlx` and passed. The gate covers upstream tokenizer, OpenAI image data URI, OpenAI video file URL, and OpenAI SSE streaming | Done for Qwen2.5-VL 3B 3bit, Qwen3-VL 4B 3bit, and SmolVLM2 256M video |
| Image-only VLM generation works end-to-end | `MLXVLM_REAL_IMAGE_MODEL=mlx-community/FastVLM-0.5B-bf16 MLXVLM_REAL_IMAGE_ALLOW_REMOTE=1 MLXVLM_REAL_IMAGE_DOWNLOAD_DIR=/Users/naen/Models/mlx-community-FastVLM-0.5B-bf16 MLXVLM_REAL_IMAGE_LABEL=FastVLM SWIFT_BUILD_JOBS=18 scripts/verify_real_vlm_image_smoke.sh` downloaded `mlx-community/FastVLM-0.5B-bf16` and passed; the gate covers upstream tokenizer, OpenAI image data URI, and OpenAI SSE streaming | Done for FastVLM 0.5B bf16 |
| OCR-style image generation works end-to-end | `MLXVLM_REAL_OCR_MODEL=/Users/naen/.cache/huggingface/hub/models--mlx-community--gemma-4-e4b-it-4bit/snapshots/cc3b666c01c20395e0dcebd53854504c7d9821f9 MLXVLM_REAL_OCR_LABEL=Gemma4-OCR MLXVLM_REAL_OCR_EXPECTED_TEXT=HELLO SWIFT_BUILD_JOBS=18 scripts/verify_real_vlm_ocr_smoke.sh` passed; the gate renders a local text image with Swift/AppKit, sends it through OpenAI Chat `image_url`, and checks the real backend generated the expected text | Done for Gemma4 OCR smoke |
| JSON mode / JSON Schema replacement is complete | Real backend has JSON root/start, first required-key prefix guidance, finite token-trie DFA masking for bounded schema alternatives, tokenizer-prefix filtering for object/array schemas with open string or number scalar slots, and deterministic full-literal token/EOS forcing when only one schema-valid path can be synthesized. `scripts/verify_real_gemma4_smoke.sh` now checks real Gemma4 JSON Schema responses with arbitrary `answer` string content plus `confidence:"low"|"high"` and a second prompt that selects the non-default `high` enum branch | Partial |
| Full JSON Schema grammar/DFA constrained decoding is available | `ResponseFormatPlan` extracts schema constraints, now includes a `JSONSchemaGrammarPlan` for deterministic property order, structural state counts, constrained scalar/container paths, enum paths, `additionalProperties:false` paths, finite token-DFA alternative counts, open string/number scalar paths, whether finite alternatives rely on synthesized scalar defaults, and whether tokenizer prefix filtering is required; `MLXVLMUpstreamBackend` compiles bounded alternatives into a token trie and uses a schema-prefix grammar processor for open scalar slots before falling back to a single deterministic literal. `StructuredOutputValidator` post-validates generated JSON Schema outputs. Remaining gaps are full JSON Schema keyword coverage and broader schema-shape verification beyond the currently supported deterministic object/array skeletons, enum/const/defaults, booleans/nulls, open strings, and open numbers | Partial |
| Python `mlx-vlm` model-family parity is complete | Gemma4 real smoke passes; Qwen2.5-VL, Qwen3-VL, SmolVLM2, and FastVLM real smokes pass through upstream `mlx-swift-lm`; Qwen VL planning/metadata is broad; non-generative Python custom predictor families are classified as explicit `501` compatibility exclusions rather than routed through generation or Python fallback | Partial |
| RF-DETR/SAM3 and other non-generative model actual inference is Swift-backed | Capability planning prevents incorrect text generation, and `/predict`, `/v1/predictions`, `/api/predict`, plus `preflight-predict`, now return typed predictor-unavailable diagnostics with media resolution, preferred entrypoints, `predictorRuntimePolicy: explicit-501-compatibility-exclusion`, `pythonRuntimeRequired: false`, and a compatibility-exclusion explanation. This repo is formally keeping those Python custom predictor pipelines outside the upstream mlx-swift-lm generative replacement scope until a native Swift predictor backend is intentionally added | Explicitly excluded |
| SentencePiece/Unigram fallback tokenizers are Swift-executable without upstream tokenizer | Unigram `tokenizer.json` and binary SentencePiece `tokenizer.model` greedy fallbacks are covered by `scripts/verify_swift_port.sh`; binary SentencePiece BPE/Word/Char variants are classified from `trainer_spec.model_type` and marked `upstreamTokenizerDelegated` for the real `mlx-swift-lm` tokenizer path instead of requiring Python | Mostly done |

## Current Gates

- `swift build --disable-sandbox --jobs 18`
- `swift run mlx-vlm-swift self-test`
- `SWIFT_BUILD_JOBS=18 scripts/verify_swift_port.sh`
- `SWIFT_BUILD_JOBS=18 scripts/verify_mock_real_mlx_api.sh`
- `SWIFT_BUILD_JOBS=18 scripts/verify_real_gemma4_smoke.sh`
- `SWIFT_BUILD_JOBS=18 scripts/verify_real_vlm_ocr_smoke.sh`
- `MLXVLM_REAL_QWEN_VL_MODEL=/path/to/qwen-vl SWIFT_BUILD_JOBS=18 scripts/verify_real_qwen25vl_video_smoke.sh`, or `MLXVLM_REAL_QWEN_VL_ALLOW_REMOTE=1 SWIFT_BUILD_JOBS=18 scripts/verify_real_qwen25vl_video_smoke.sh` when remote Hugging Face downloads are acceptable. Legacy `MLXVLM_REAL_QWEN25VL_*` variable names are still accepted.
- `MLXVLM_REAL_IMAGE_MODEL=/path/to/image-vlm SWIFT_BUILD_JOBS=18 scripts/verify_real_vlm_image_smoke.sh`, or `MLXVLM_REAL_IMAGE_ALLOW_REMOTE=1 SWIFT_BUILD_JOBS=18 scripts/verify_real_vlm_image_smoke.sh` when remote Hugging Face downloads are acceptable.

These gates are necessary but not sufficient for 100% completion because the set still lacks exhaustive JSON Schema keyword/model-family coverage.

## Next Required Work

1. Broaden the JSON Schema grammar beyond the current deterministic object/array skeleton support: verify nested open scalar schemas, numeric constraints, optional non-default properties, unions, string patterns/formats, and streaming behavior across Gemma4 and at least one Qwen-family model without relying on synthesized scalar defaults.
